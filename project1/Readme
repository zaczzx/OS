1. Main loop (done)
2. Foreground jobs (done)

e.g.
cp file1 file2
cp -R dir1 dir2
rm -f -R dir1 fir2
xterm

3. Background jobs (done)
e.g.
cp file1 file2 &
cp -R dir1 dir2 &
rm -f -R dir1 dir2 &
xterm &

4. Redirection of standard input and output (done)
e.g.
ls -a -l -F > file1
ls -a -l -F > file2 &
cat -n < file1
cat < file1 > file2
cat > file3 < file1
cat < bogusfile

5. Built-in-commands (done)
e.g.
cd newdir
cd ../
xterm &
wait xterm
exit

6. Pipes (doing, bug fix)
